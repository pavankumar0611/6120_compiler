@main {
  a: int = const 4;
  b: int = const 4;
  c: int = const 1;
  d: int = add  a b;
  e: int = add  b a;
  print d  e; # here 'e'  should replace with print d ,since both( d , e) are pointing to same value in table
}
